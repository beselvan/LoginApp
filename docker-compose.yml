version: '3'
services:
  web:
    container_name: tomcat
    dockerfile: ./Dockerfile_tomcat
    restart: always
    ports:
     - "8081:8080"
    links:
     - dbserver
  dbserver:
    dockerfile: ./Dockerfile_mysql
    container_name: mysql
    restart: always
    ports:
     - "3306:3306"
